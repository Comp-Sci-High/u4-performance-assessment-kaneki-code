<!-- https://chatgpt.com/share/67a033b2-7d68-800e-8d2f-579cea5c4185 -->
<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Quoted Horror - Horror Books</title>
    <link rel="stylesheet" href="/book.css">
</head>
<body>
    <!-- Header Section -->
    <header>
        <h1 class="logo">Quoted Horror</h1>
        <nav>
            <a href="/">Home</a>
            <a href="/books">Horror Books</a>
        </nav>
    </header>

    <!-- Book Scroll Container -->
    <div class="book-scroll-container">
        <% for(i = 0; i < items.length; i++) { %>
            <a href="/books/<%= i %>" class="book-wrapper">
                <div class="book-card">
                    <div class="book-cover" style="background-image: url('<%=items[i].image%>');"></div>
                    <div class="book-info">
                        <h3><%=items[i].name%></h3>
                        <p><%=items[i].author%></p>
                        <blockquote><%=items[i].quote %></blockquote>
                    </div>
                </div>
            </a>
        <% } %>
    </div>
    <div class="book-scroll-container">
        <% for(i = 0; i < items.length; i++) { %>
            <a href="/books/<%= i %>" class="book-wrapper">
                <div class="book-card">
                    <div class="book-cover" style="background-image: url('<%=items[i].image%>');"></div>
                    <div class="book-info">
                        <h3><%=items[i].name%></h3>
                        <p><%=items[i].author%></p>
                        <blockquote><%=items[i].quote %></blockquote>
                    </div>
                </div>
            </a>
        <% } %>
    </div>
</body>
</html>